
  # Create UI with Text

  This is a code bundle for Create UI with Text. The original project is available at https://www.figma.com/design/oA1WZmq429IUAlgithfEYM/Create-UI-with-Text.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  